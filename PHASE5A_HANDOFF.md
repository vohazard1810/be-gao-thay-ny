# Phase 5A handoff

## Scope hoàn thành

- Thay ảnh sai của `farm_chick` bằng ảnh chụp gà con cục bộ.
- Thêm chế độ `compact` cho Thầy Ny để crop nửa người ở vùng hiển thị nhỏ.
- Tăng kích thước Thầy Ny tại hero card, flashcard và khung lời thoại.
- Đổi chỉ báo danh mục thành pill `Vuốt ›` dễ nhận biết.
- Đổi icon loa nghỉ sang icon Replay; màu teal khi đang phát, không dùng đỏ cảnh báo.
- Thêm `speakerId` cho đủ 16 cảnh của 4 truyện đã publish.
- Hiển thị màu/tên người đang nói và thay đổi nhẹ pitch/rate TTS theo nhân vật.
- Bổ sung unit test cho ảnh Gà Con và speaker mapping.

## Chưa nằm trong Phase 5A

- Game Hub và game Ghép Cặp.
- Dashboard tiến trình dành cho phụ huynh.
- Audio lồng tiếng thu sẵn cho từng nhân vật.

Các mục trên chỉ bắt đầu sau khi Phase 5A build xanh và được xem lại bằng video runtime.
